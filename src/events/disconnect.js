"use strict";
//import { Client, CloseEvent } from "discord.js";
// /**
//  * Emitted whenever the client failed to reconnect multiple times and is giving up - time to restart with forever
//  * @param {Client} client discord client
//  * @param {CloseEvent} event The disconnect event
//  */
// module.exports = async (client: Client, event: CloseEvent) => {
//   console.log(
//     `The WebSocket has closed and will no longer attempt to reconnect - exiting node`
//   );
//   return process.exit(1);
// };
